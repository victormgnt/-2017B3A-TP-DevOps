FROM node:8.9.3

WORKDIR /usr/local

COPY wik-devops-3tp ./
RUN npm install --production

ENTRYPOINT NODE_ENV=$NODE_ENV MONGO_HOST=$MONGO_HOST MONGO_USER=$MONGO_USER MONGO_DATABASE=test MONGO_PASSWORD=$MONGO_PASSWORD node app.js
